// Basic test module for wfc-gpu crate
// This file satisfies the `mod tests;` declaration in lib.rs

#[cfg(test)]
mod unit_tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}

// Place for other unit tests to be added
// For integration tests, use the tests directory at the crate root
